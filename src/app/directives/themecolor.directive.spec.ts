import { ThemecolorDirective } from './themecolor.directive';

describe('ThemecolorDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemecolorDirective();
    expect(directive).toBeTruthy();
  });
});
