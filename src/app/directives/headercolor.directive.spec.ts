import { HeadercolorDirective } from './headercolor.directive';

describe('HeadercolorDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadercolorDirective();
    expect(directive).toBeTruthy();
  });
});
